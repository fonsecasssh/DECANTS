# DECANT LAB — Render Final

Estrutura preparada para o repositório `fonsecassh/DECANTS`.

Arquivos que devem ficar NA RAIZ do GitHub:
- index.html
- render.yaml
- README.md
- .gitignore

## Render
O Blueprint já está configurado com:
`staticPublishPath: .`

Depois de enviar os arquivos ao GitHub:
1. Confirme que `index.html` aparece diretamente na página inicial do repositório.
2. O Render deve sincronizar o Blueprint automaticamente.
3. Se necessário: Manual Deploy > Deploy latest commit.
